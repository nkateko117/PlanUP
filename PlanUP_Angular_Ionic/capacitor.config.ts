import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nkateko117.PlanUP',
  appName: 'PlanUP App',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
